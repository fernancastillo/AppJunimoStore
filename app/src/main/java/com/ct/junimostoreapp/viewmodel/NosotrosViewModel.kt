package com.ct.junimostoreapp.viewmodel

import androidx.lifecycle.ViewModel

class NosotrosViewModel : ViewModel() { // ViewModel para la pantalla "Sobre Nosotros".
    // Lógica de la pantalla Nosotros (actualmente vacía, pero puede contener estados y eventos futuros).
}