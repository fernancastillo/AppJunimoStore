package com.ct.junimostoreapp.viewmodel

import androidx.lifecycle.ViewModel

class BlogViewModel : ViewModel() { // ViewModel para la pantalla de blogs.
    // Lógica de la pantalla de blogs (actualmente vacía, pero puede contener estados y eventos futuros).
}