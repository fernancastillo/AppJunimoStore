package com.ct.junimostoreapp.ui.home

import androidx.lifecycle.ViewModel

class IndexViewModel : ViewModel() { // ViewModel para la pantalla de inicio.
    // Lógica para la pantalla principal (actualmente vacía, pero puede contener estados y eventos futuros).
}